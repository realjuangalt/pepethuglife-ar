{
  "name": "pepethuglife-static-ar",
  "version": "1.0.0",
  "description": "Static MindAR + A-Frame marker AR for GitHub Pages (repo root).",
  "private": true,
  "scripts": {
    "preview": "python3 -m http.server 8080",
    "build": "echo 'No build step — deploy the repo root as static files.'"
  },
  "keywords": ["ar", "mindar", "aframe", "github-pages"],
  "license": "UNLICENSED"
}
