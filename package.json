{
  "name": "pepethuglife-static-ar",
  "version": "1.0.0",
  "description": "Static MindAR + video marker presentation (GitHub Pages). Legacy Python backend optional/unused for static deploy.",
  "private": true,
  "scripts": {
    "start": "python3 start_backend.py",
    "dev": "python3 start_backend.py",
    "backend": "cd backend && python3 app.py",
    "build": "echo 'Static site: deploy frontend/ only'",
    "test": "python3 -m pytest tests/",
    "install": "pip3 install -r requirements-python.txt"
  },
  "keywords": [
    "ar",
    "mindar",
    "aframe",
    "github-pages"
  ],
  "engines": {
    "node": ">=18"
  }
}
